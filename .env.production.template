# Environment Variables for Vercel Deployment
# Add these in Vercel Dashboard: Project Settings > Environment Variables

# Frontend API Base URL (update with your actual Vercel domain)
REACT_APP_API_BASE_URL=https://your-vercel-domain.vercel.app/api

# Backend Environment Variables
NODE_ENV=production

# MongoDB Connection (your existing URI)
MONGO_URI=YOUR_MONGODB_ATLAS_URI

# JWT Secret (set this to a strong random value in Vercel env)
JWT_SECRET=YOUR_SECURE_RANDOM_JWT_SECRET

# Port (Vercel handles this automatically)
PORT=5000

# IMPORTANT: 
# 1. Do NOT commit actual secrets
# 2. Add these variables in Vercel Dashboard instead
# 3. Update REACT_APP_API_BASE_URL with your actual Vercel domain after first deployment
